//Base URL for the API
// For a real application, this could be set dynamically using environment variables (.env file) so that it can be different in development and production environments.

export const API_BASE_URL = "http://localhost:3000"; // Replace with your API base URL
